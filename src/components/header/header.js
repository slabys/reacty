import React, {Component} from 'react';
import App from "../../App";

function Header(){
    return (
        <header className="Header">
            <p>Header</p>
        </header>);
}

export default Header;